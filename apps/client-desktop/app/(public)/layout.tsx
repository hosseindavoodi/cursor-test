import { ReactNode } from "react";
import Navbar from "../_components/navbar";

function LandingsLayout({ children }: { children: ReactNode }) {
  return <div>{children}</div>;
}

export default LandingsLayout;
